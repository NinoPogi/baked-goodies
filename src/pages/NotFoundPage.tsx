import { VStack, Heading } from "@chakra-ui/react";

const NotFoundPage = () => {
  return (
    <VStack>
      <Heading>Page Not Found!!</Heading>
    </VStack>
  );
};

export default NotFoundPage;
